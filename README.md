A simple C++ weather app that:
  -Saves location in file and loads location when you open the app
  -Displays weather info based off location
  -Allows user to edit their location

Required libraries:
  -CURL
  -SFML
